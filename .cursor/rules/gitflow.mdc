---
description: 远程只保留 master 与 develop；功能走 feature_*，修 bug 走 fix_*，合并后删除主题分支。
alwaysApply: true
---

# GitFlow

实现新功能或修复缺陷时，先读并遵循 `.cursor/skills/gitflow-branching/SKILL.md`：

- 远程稳定分支只有 `master`（发布）和 `develop`（集成）
- 新功能：从 `develop` 创建 `feature_<slug>`，合回 `develop` 后删除
- 修 bug：从 `develop` 创建 `fix_<slug>`，合回 `develop` 后删除；若修的是已发布 `master`，从 `master` 切出，合回 `master` 和 `develop` 后再删
- 不要在 `master` 或 `develop` 上直接提交
- 不要用 `feature/`、`bugfix/`、`hotfix/` 这种斜杠命名
