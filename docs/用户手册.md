# 仿真模拟器 — 用户手册

面向 EMS/BMS/PCS 联调与测试人员。说明安装、启动、界面、Modbus 联调及常见问题；内部实现见 [系统设计说明](./系统设计说明.md)。

---

## 1. 产品简介

仿真模拟器在本地模拟电池、变流器、变压器、断路器及并网点电气行为，通过 **Modbus TCP** 提供与现场相近的遥测与控制接口，便于无真机或真机不足时完成主控联调。

- 对外名称：**仿真模拟器**
- 发布包为自包含程序，**无需单独安装 .NET 运行时**

---

## 2. 系统要求

| 平台 | 要求 |
|------|------|
| Windows | Windows 10/11 或 Server 2019+，64 位 (x64) |
| Linux | 与发布包架构一致（如 linux-arm64） |
| macOS | 社区/演示包提供 osx-arm64；需按随包说明解除隔离 |
| 内存 | 建议 4 GB 及以上 |
| 浏览器 | 操作界面为 Web（默认端口 5050） |
| 网络 | 联调时 Modbus 端口未被占用 |

---

## 3. 快速启动

### Windows

1. 解压整个文件夹（exe、配置、点表同目录）
2. 双击 `start.bat` 或运行 `EssSimulator.exe`
3. 等待终端出现协议就绪后，浏览器打开 **http://127.0.0.1:5050**（发布脚本通常会自动打开）

### Linux

```bash
chmod +x EssSimulator start.sh
./start.sh
```

### 开发环境

```bash
dotnet run --project EssSimulator.csproj
# 或一键前后端：./scripts/dev-up.sh
```

浏览器访问 `http://127.0.0.1:5050`（开发前端 HMR 为 `http://localhost:5173`）。

---

## 4. 目录与配置

| 文件 | 说明 |
|------|------|
| `EssSimulator.exe` / `EssSimulator` | 主程序 |
| `appsettings.json` | 单元数、端口、电气参数 |
| `emu.csv` / `em.csv` | PCS·EMU、电表点表（源文件在 `pointmaps/{版本}/`） |
| `bms_bank.csv` / `bms_rack.csv` | BMS 堆、簇点表 |
| `autotest.json` | `dpctest` 自动化脚本 |
| `log4net.config` | 日志 |

- 修改 `appsettings.json` 后需 **重启**
- 点位表版本见 [pointmaps/README.md](../pointmaps/README.md)；本地切换：`./scripts/sync-pointmaps-to-root.sh [版本]`
- 配置项详解：[appsettings 字段说明](./appsettings.explained.md)

---

## 5. Modbus TCP 端口

在 `appsettings.json` → `Simulator.Protocol` 修改。常见默认：

| 设备 | 服务名 | 默认端口 | 点表 |
|------|--------|----------|------|
| 并网点电表 | simEm | 1500 | em.csv |
| BMS 第 N 路 | simBmsN | 1501+(N-1) | bms_bank.csv |
| EMU 第 U 单元 | simEmuU | 1600+U | emu.csv |

- 从站地址（Unit ID）一般为 **1**
- BMS 簇表从站：**1 + 簇序号**（见 `bms_rack.csv`）
- 默认监听 **127.0.0.1**；跨机联调需改绑定并放行防火墙

命令与 mbpoll 示例见 [指令详细说明](./指令详细说明.md)。

---

## 6. Web 界面

启动后用浏览器操作（侧栏菜单）。社区版会隐藏组态 / 3D / 白盒切片。

| 页面 | 作用 |
|------|------|
| 主电气接线 | 220 kV / 35 kV / 690 V 拓扑、PCS/BMS、P设/P实 |
| 主接线 3D | 三维站场（商业/演示/定制） |
| 组态编辑 / 工程管理 / 系统配置 | 拓扑画布、工程落地为运行时 overlay（工程模式）、设备型号点表选型 |
| 电池堆簇信息 | 按 BMS 路号查看 SOC、电压、电流 |
| 电池单体信息 | 选定簇的单体电压 |
| BMS 告警门限 / 设备告警 | 簇门限与故障位 |
| 命令输入 | esscmd / dpc / breaker / dpctest |
| 白盒切片 | EMS 写有功/无功时的瞬时快照 |
| 连接信息 | Modbus 监听与客户端 |

命令也可在启动仿真的终端窗口输入。架构见 [B/S 架构说明](./B-S架构说明.md)。

### 6.1 设备型号与点位表选择（系统配置页）

不同设备型号使用不同的 Modbus 点位表。系统配置页「设备型号与点位表」卡片按设备类型
（BMS / EMU / EM / LocalControl）列出可选型号，选择后点「确认并重新初始化」，
选型写入 `configs/topology/device-models.json`，后端重启后按选中型号的点表重建 Modbus 服务。

- 型号点表位于 `pointmaps/models/{设备类型}/{型号}/`，新增型号只需放入点表文件与 `model.json`，无需改代码。
- 未选型的设备类型仍按兜底点表解析（工作目录根下 CSV / `pointmaps` 版本目录），行为与旧版一致。
- PV 点表暂未纳入型号体系。

### 6.2 组态编辑（拓扑画布）

组态画布上拖入设备、拉线完成站侧接线；EMU 储能单元为**虚拟节点**：

- **EMU 储能单元**：拖入后不在画布显示图形，由左侧「EMU 储能单元」列表面板管理
  （新增 / 重命名 / 删除 / 改单元变参数）；面板同时展示该 EMU 绑定的断路器 / 电表（未绑定显示「未绑定」）；
  删除 EMU 会自动解除归属它的 PCS、断路器与电表。
- **PCS 变流器**：独立设备节点，上接三相 AC（35 kV 母线）、下接直流（BMS/DC 母线）；
  参数「所属 EMU 储能单元」下拉框选择归属的 EMU，选择后该 PCS 计入该 EMU。
- **断路器 / 电表绑定**：三相断路器、三相电表同样通过参数「所属 EMU 储能单元」归入 EMU，
  绑定**可选**，且每个 EMU 至多各绑 1 台。绑定后：
  - 电站概览中该断路器 / 电表以真实节点身份画入 EMU 虚线框（虚线框包裹整单元：
    断路器 → 单元变 → 690 母线 → PCS → DC 母线 → BMS）；未绑定则沿用合成符号或不画。
  - 数字孪生（3D）不引入 EMU 概念，按组态逐设备绘制：绑定的断路器 / 电表、每台 PCS / BMS
    各自独立出模型；未绑定的设备不画。
- **保存校验**：每台 PCS 必须选择有效的所属 EMU；断路器 / 电表已选 emuId 时必须指向存在的 EMU，
  且同一 EMU 下断路器 / 电表各至多 1 台（超出报 `EMU_BREAKER_DUPLICATE` / `EMU_METER_DUPLICATE`）；
  工程至少需要一个含 PCS 的 EMU 或光伏单元。
- **应用到仿真**：按 EMU 分组生成储能单元，每台 PCS 沿其直流连线配 1 台 BMS（1:1 对齐）；
  绑定的断路器 / 电表记录为单元的 `HasUnitBreaker` / `HasUnitMeter` 构成。
- **点表绑定门控**：EMU（simEmu）与 LocalControl（simLc）点表的 ModelSim 绑定只能指向机组内
  实际存在的设备模型：`PcsList[i]` 路径要求 i 小于该机组 PCS 台数，`ElectricityMeter` 路径要求
  机组绑定电表，`Emu.PowerOnOff`（单元高压断路器开合）要求机组绑定断路器；找不到对应设备的点位
  在目录编译期剔除，按未绑定点位处理（寄存器仅维持点表默认值，遥测不刷新、控制写入不生效）。
  无组态 overlay 的纯 appsettings 部署不门控，行为与旧版一致。
- 向导（新建工程）仍按 EMU 单元数生成，每个 EMU 默认含 2 台 PCS。

注意：旧版以 EMU 为连线节点的工程不再兼容，需按新模型重建。

---

## 7. 典型联调顺序

```text
dpc simEmu1.yx0 set 1          # 单元高压合闸
esscmd setbms1 power on        # BMS 直流并网
dpc simEmu1.yx3 set 1          # PCS1 启动
dpc simEmu1.yt0 set 1000       # 有功 100 kW（Scale 10）
```

在主接线查看 **P设 / P实**，或用 mbpoll 读 `yc27` 等反馈点。

---

## 8. 无界面 / 仅协议

TUI 已移除。`Runtime.NoGui` 仅兼容旧配置，**不再关闭 Web**。  
若发布包关闭了静态页（`Simulator.Web.StaticFiles=false`），仍提供 Modbus 与 `/api/*`，用 EMS/mbpoll 或自行接前端。

---

## 9. 常见问题

| 现象 | 处理 |
|------|------|
| Modbus 连不上 | 等协议就绪；查端口、防火墙、IP 绑定 |
| PCS 启停无效 | 查 yx0 合闸、BMS 并网、`esscmd setbms1 power on` |
| P实 为 0 | 确认 PCS 运行且并网；注意 Scale（100 kW → 写 1000） |
| dpc 功率差 10 倍 | 控制点写 **原始寄存器值**，非工程 kW |
| link 后 SOH 等为 0 | 重连后会刷新；仍异常则重启或查 [指令说明](./指令详细说明.md) |
| 改单元数无法启动 | 检查 JSON 语法、EssUnits、端口冲突 |

---

## 10. 发布与版本

社区版 / 商业版 / 定制版 / 演示版目录与打包见 [项目编译说明](./项目编译说明.md)。分档见 [产品分档与交付边界](./产品分档与交付边界.md)。

随包 `README.txt` 为社区版简明说明；完整命令见 [指令详细说明](./指令详细说明.md)。

---

## 11. 文档索引

见 [docs/README.md](./README.md)。
