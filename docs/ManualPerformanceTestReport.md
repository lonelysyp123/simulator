# EssSimulator 手动性能测试报告

> 测试类型：mbpoll / dpc 变位 → 监控响应延迟（**同点** + **PCS 控制→反馈**）  
> **developer_v2.0.0**：点表与 develop 一致（yx5/yx1/yt4/yt2/yx3/yc44）；脚本位于 `scripts/perf/`  
> **代跑时间**：2026-06-22 17:18～17:19（优化后：ControlEventDriven + 控制后立即刷 OperationStatus 遥测）

---

## 1. 测试基本信息

| 项目 | 内容 |
|------|------|
| 测试人员 | 宋尹培 |
| 测试日期 | 2026/06/22 |
| 被测分支 / Commit | **developer_v2.0.0** |
| 配置文件 | ☑ `appsettings.json` |
| DataExchange | BMS `TelemetryIntervalMs=500`，EMU `EmuTelemetryIntervalMs=100`，`ControlEventDriven=true` |
| 单元规模 | 8个单元 |
| 终端环境 | 本机 |
| 仿真参数 | SimStepMs=200，Speedup=1.0，NoGui=false |
| mbpoll 版本 | 当前版本 |
| mbpoll 监控轮询间隔 | 50 ms（脚本 `run_tc_r01.py`～`run_tc_r05_pcs.py`，`sleep 0.05`） |
| 测试结论 | ☑ 通过 ☐ 部分通过 ☐ 不通过 |

---

## 2. 本次测试范围

| 编号 | 测试项 | 触发方式 | 监控方式 | 验收标准 |
|------|--------|----------|----------|----------|
| TC-R01 | BMS bank 一键并网（**yc133**） | 脚本写 FC6 寄存器 | mbpoll 读 **同一点** | ΔT ≤ 700 ms |
| TC-R02 | EMU 高压断路器（**highvoltagebreakeronoff**） | 脚本写线圈 | mbpoll 读 **同一点** | ΔT ≤ 700 ms |
| TC-R03 | BMS rack 总电压过压门限（**yc1326**） | 脚本写 FC6 | mbpoll 读 **同一点** rack | ΔT ≤ 700 ms |
| TC-R04 | EMU 功率因数设定（**param57**） | 脚本写 FC6 | mbpoll 读 **同一点** | ΔT ≤ 700 ms |
| TC-R05 | PCS1 启动（**pcs1_startstop→param25**） | mbpoll 写启停线圈 | mbpoll 读 **运行状态 param25** | ΔT_status ≤ 700 ms |
| TC-R06 | PCS1 停机（**pcs1_startstop→param25**） | mbpoll 写启停线圈 | mbpoll 读 **运行状态 param25** | ΔT_status ≤ 700 ms |

**计时规则**

| 符号 | 含义 |
|------|------|
| T0 | GUI「命令输入」中 dpc 回显时间戳（`HH:mm:ss.fff`） |
| T1 | mbpoll 监控终端**首次**读到与预期一致的值（线圈 0/1 或寄存器 raw 值） |
| ΔT | T1 − T0（含约 ±50 ms 轮询误差） |

**同点原则**（TC-R01～R04）：触发点位与 mbpoll 监控点位必须一致（改哪个点、读哪个点），不跨 bank/rack 做联动观测。

**控制→反馈原则**（TC-R05～R06）：mbpoll 写 **pcs1_startstop**，监控 **param25**（`OperationStatus`）。v2.0.0 经 **DataExchange** 控制管道 + **EmuPcsControlEffect** 立即 `ApplyEmuCommands`，遥测由 **EmuTelemetryIntervalMs(100ms)** 刷新。

| 符号 | 含义 |
|------|------|
| ΔT_coil | T0 写线圈后，pcs1_startstop 读回一致 |
| ΔT_status | T0 写线圈后，param25 首次达预期状态码 |

---

## 3. 测试前准备

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 仿真器已启动，可进入 GUI「命令输入」 | ☑ 是 ☐ 否 | GUI 主电气接线界面运行中 |
| `simBms1` 端口 1501 可连接 | ☑ 是 ☐ 否 | `nc -z 127.0.0.1 1501` |
| `simEmu1` 端口 1601 可连接 | ☑ 是 ☐ 否 | |
| 已确认 BMS rack 从站号规则 | ☑ 是 ☐ 否 | 簇 0 → slave 2 |

### 3.1 BMS rack 从站号说明

BMS 主站 `slave=1` 对应 `bms_bank.csv`；**rack 簇 n** 对应 **slave = n + 1**：

| Rack 簇 | mbpoll `-a` | 说明 |
|---------|-------------|------|
| 簇 0 | **2** | 第 1 个 rack 从站 |
| 簇 1 | **3** | 第 2 个 rack 从站 |
| 簇 k | k + 2 | 以此类推 |

本报告 **TC-R01** 使用 **bank 主站 slave 1**；**TC-R03** 使用 **rack 簇 0 → slave 2**（BMS bank 主站无 FC6 遥调点，模拟量选用 rack 遥调寄存器）。

---

## 4. 监控点位与命令

### 4.1 TC-R01 监控：Bank 一键并网（yc133）

| 项 | 值 |
|----|-----|
| 设备 | simBms1 |
| 点表 | `bms_bank.csv` |
| 点名 | **yc133** |
| 含义 | 一键并网开关（写 1 触发） |
| FC | 6 → mbpoll **`-t 4`** |
| 地址 | **1001** |
| 端口 / 从站 | **1501** / **1** |
| dpc 触发 | `dpc simBms1.yc133 set 1` |
| 脚本 | `python3 scripts/perf/run_tc_r01.py` |

```bash
while true; do
  date '+%H:%M:%S.%3N'
  mbpoll -0 -t 4 -a 1 -r 1001 -c 1 -p 1501 -1 -q 127.0.0.1
  sleep 0.05
done
```

### 4.2 TC-R02 监控：EMU 高压断路器（highvoltagebreakeronoff）

| 项 | 值 |
|----|-----|
| 设备 | simEmu1 |
| 点名 | **highvoltagebreakeronoff** |
| FC | 5 → **`-t 0`** |
| 地址 | **4** |
| 端口 / 从站 | **1601** / **1** |
| 模型 | `emu.Emu.PowerOnOff` |
| dpc | `dpc simEmu1.highvoltagebreakeronoff set 1/0` |
| 脚本 | `python3 scripts/perf/run_tc_r02.py` |

```bash
while true; do
  date '+%H:%M:%S.%3N'
  mbpoll -0 -t 0 -a 1 -r 4 -c 1 -p 1601 -1 -q 127.0.0.1
  sleep 0.05
done
```

### 4.3 TC-R03 监控：Rack 总电压过压门限（yc1326）

| 项 | 值 |
|----|-----|
| 设备 | simBms1 rack 簇 0 |
| 点名 | **yc1326** |
| FC | 6 → **`-t 4`** |
| 地址 | **12293** |
| 端口 / 从站 | **1501** / **2** |
| Scale | **10** |
| 脚本 | `python3 scripts/perf/run_tc_r03.py` |

```bash
while true; do
  date '+%H:%M:%S.%3N'
  mbpoll -0 -t 4 -a 2 -r 12293 -c 1 -p 1501 -1 -q 127.0.0.1
  sleep 0.05
done
```

### 4.4 TC-R04 监控：EMU 功率因数（param57）

| 项 | 值 |
|----|-----|
| 设备 | simEmu1 |
| 点名 | **param57** |
| FC | 6 → **`-t 4`** |
| 地址 | **5302** |
| 端口 / 从站 | **1601** / **1** |
| Scale | **1000** |
| 脚本 | `python3 scripts/perf/run_tc_r04.py` |

```bash
while true; do
  date '+%H:%M:%S.%3N'
  mbpoll -0 -t 4 -a 1 -r 5302 -c 1 -p 1601 -1 -q 127.0.0.1
  sleep 0.05
done
```

### 4.5 TC-R05/R06：PCS1 启停与运行状态

| 项 | 控制（写） | 反馈（读） |
|----|------------|------------|
| 点名 | **pcs1_startstop** | **param25** |
| FC / 地址 | 5 / **5303** | 4 / **25333** |
| 端口 / 从站 | 1601 / 1 | 1601 / 1 |
| 有功预设 | **param55** @ **5300** raw=500 | — |
| 启动期望 | 写 1 | param25 ∈ **{1,3,4}** |
| 停机期望 | 写 0 | param25 = **0** |

**param25 状态码**：0 停机 · 1 待机 · 2 故障 · 3 充电 · 4 放电

```bash
while true; do
  date '+%H:%M:%S.%3N'
  echo -n "start="; mbpoll -0 -t 0 -a 1 -r 5303 -c 1 -p 1601 -1 -q 127.0.0.1
  echo -n " status="; mbpoll -0 -t 3 -a 1 -r 25333 -c 1 -p 1601 -1 -q 127.0.0.1
  sleep 0.05
done
```

**测试前条件**：单元 **highvoltagebreakeronoff=1**、BMS1 **并网**、启动前 param25 ≠ 2。

**理论路径**：mbpoll 写 pcs1_startstop → DataExchange 控制(100ms) → **EmuPcsControlEffect** → PCS 模型变位 → PcsDataServer(100ms) → **EMU 遥测(100ms)** → param25 可读。**ΔT_status 预期 ≤ 300 ms**。

**脚本**：`python3 scripts/perf/run_tc_r05_pcs.py`

---

## 5. 响应速度测试（同点触发 → mbpoll）

> **5.1 / 5.2** 为**数字量**同点测试（线圈 yx）；**5.3 / 5.4** 为**模拟量**同点测试（yt，FC6 / `-t 4`）；**5.5** 为 **PCS 控制→反馈**测试（写 yx3，读 yc44）。

### 5.1 TC-R01 — BMS bank yx5 变位响应

#### 用例定义

| 项 | 内容 |
|----|------|
| 目的 | 测量触发 **bank yx5** 后，**同一点位**在 Modbus 上反映的延迟 |
| 触发点 | simBms1，slave **1**，yx5，地址 **1005**，端口 **1501** |
| 监控点 | **与触发点相同**（yx5 @1501/a1/1005） |
| 理论路径（dpc） | dpc 写模型 → DataWorker(500ms) → mbpoll 读到 |
| 理论路径（mbpoll 写线圈） | Modbus 控制线程(100ms) → 线圈可读 |
| 预期 ΔT | ≤ **700 ms** |

#### 触发与复位

| 项 | 内容 |
|----|------|
| dpc 置位 | `dpc simBms1.yx5 set 1` |
| dpc 复位 | `dpc simBms1.yx5 set 0` |
| 映射路径 | `bms1.BatteryStacks[0].SystemAlarms.OvervoltageFault` |
| 脚本代跑 | `python3 scripts/perf/run_tc_r01.py`（mbpoll 写 addr 1005，监控同址） |

#### 操作步骤

1. 终端 B 启动 4.1 节 **yx5** 监控循环，确认初始值为 **0**。
2. GUI →「命令输入」，或运行代跑脚本。
3. 执行 `dpc simBms1.yx5 set 1`（或脚本写线圈），记录 **T0**。
4. 观察终端 B，记录 yx5 首次变为 **1** 的 **T1**，计算 **ΔT = T1 − T0**。
5. 执行复位 `dpc simBms1.yx5 set 0`，记录 yx5 回 **0** 的 ΔT（可选）。
6. 重复步骤 3–5 共 **3 次**。

#### 执行记录（置位 set 1）

> **代跑说明（2026-06-22）**：脚本 `run_tc_r01.py` 通过 mbpoll 写 bank 线圈 addr 1005=1，**T0 为写线圈完成时刻**；监控点与触发点均为 **yx5（slave 1 @1005）**。GUI 手动测试时 T0 取 dpc 回显时间戳。

| 次数 | T0（触发时刻） | 操作 | T1（yx5 首次=1） | ΔT（ms） | 复位 ΔT（ms） | 结果 |
|------|----------------|------|------------------|----------|---------------|------|
| 1 | 17:18:57.843 | yx5 set 1 | 17:18:57.872 | 29 | 30 | ☑通过 ☐失败 |
| 2 | 17:18:58.618 | yx5 set 1 | 17:18:58.653 | 35 | 29 | ☑通过 ☐失败 |
| 3 | 17:18:59.417 | yx5 set 1 | 17:18:59.446 | 29 | 27 | ☑通过 ☐失败 |
| **统计** | — | — | — | avg=**31** max=**35** min=**29** | avg=**29** | ☑通过 |

#### 现象描述

1. **触发与监控均为 bank yx5**，置位后约 **29–35 ms** 内 mbpoll 可读回为 1，复位同理，**满足 ≤700 ms**。
2. 本次代跑为 **mbpoll 写线圈** 路径（控制线程 ~100 ms 量级），延迟低于纯 dpc→DataWorker(500ms) 理论上限；若需测 dpc 完整路径，请在 GUI 命令输入手动执行 dpc 并保留 4.1 监控循环。
3. 本用例**不**观测 rack 从站或其他 bank 点位，避免跨层级联动干扰结论。

---

### 5.2 TC-R02 — EMU 低压断路器（yx1）开合

#### 用例定义

| 项 | 内容 |
|----|------|
| 目的 | 测量触发 **yx1** 后，**同一点位**在 Modbus 上反映的延迟 |
| 触发点 | simEmu1，slave **1**，yx1，地址 **1001**，端口 **1601** |
| 监控点 | **与触发点相同**（yx1 @1601/a1/1001） |
| dpc 合闸 | `dpc simEmu1.yx1 set 1` |
| dpc 分闸 | `dpc simEmu1.yx1 set 0` |
| 理论路径（mbpoll 写线圈） | Modbus 线圈直写 → 同址读回 |
| 预期 ΔT | ≤ **700 ms** |
| 脚本代跑 | `python3 scripts/perf/run_tc_r02.py` |

#### 操作步骤

1. 终端 B 启动 4.2 节 **yx1** 监控循环，确认初始值为 **0**。
2. GUI →「命令输入」，或运行代跑脚本。
3. 合闸：执行 `dpc simEmu1.yx1 set 1`（或脚本写 1），记录 **T0**，观察 **T1**（首次读到 1），计算 ΔT。
4. 分闸：执行 `dpc simEmu1.yx1 set 0`（或脚本写 0），同样记录 ΔT。
5. 合闸 / 分闸各重复 **3 次**。

#### 执行记录 — 合闸（set 1）

> **代跑说明（2026-06-22）**：脚本 `run_tc_r02.py` 通过 mbpoll 写 EMU 线圈 addr 1001=1，**T0 为写线圈完成时刻**；监控点与触发点均为 **yx1（slave 1 @1001）**。

| 次数 | T0 | T1 | ΔT（ms） | 结果 |
|------|-----|-----|----------|------|
| 1 | 17:19:00.091 | 17:19:00.118 | 27 | ☑通过 ☐失败 |
| 2 | 17:19:00.605 | 17:19:00.630 | 25 | ☑通过 ☐失败 |
| 3 | 17:19:01.131 | 17:19:01.158 | 27 | ☑通过 ☐失败 |
| **统计** | — | — | avg=**26** max=**27** min=**25** | ☑通过 |

#### 执行记录 — 分闸（set 0）

| 次数 | T0 | T1 | ΔT（ms） | 结果 |
|------|-----|-----|----------|------|
| 1 | 17:19:01.649 | 17:19:01.679 | 30 | ☑通过 ☐失败 |
| 2 | 17:19:02.194 | 17:19:02.226 | 31 | ☑通过 ☐失败 |
| 3 | 17:19:02.715 | 17:19:02.743 | 28 | ☑通过 ☐失败 |
| **统计** | — | — | avg=**29** max=**31** min=**28** | ☑通过 |

#### 现象描述

1. **触发与监控均为 yx1**，合闸 / 分闸 ΔT 均在 **25–31 ms**，**满足 ≤700 ms**。
2. yx1 **ModelSim=0**，mbpoll 写线圈为 Modbus 层直写路径，响应快于经模型同步的 bank 遥信（DataWorker 500 ms 周期）。
3. 若需验证 dpc 路径，请在 GUI 命令输入手动执行 `dpc simEmu1.yx1 set 1/0` 并对照 4.2 监控循环（dpc 对未绑模点位可能仅写寄存器影子，行为以实测为准）。

---

### 5.3 TC-R03 — BMS rack yt4 模拟量变位响应

#### 用例定义

| 项 | 内容 |
|----|------|
| 目的 | 测量写入 **rack yt4** 后，**同一点位**在 Modbus 上反映的延迟 |
| 触发点 | simBms1 rack 簇0，slave **2**，yt4，地址 **40004**，端口 **1501** |
| 监控点 | **与触发点相同**（yt4 @1501/a2/40004，mbpoll `-t 4`） |
| 类型 | 模拟量 / 遥调（FC6 保持寄存器） |
| Scale | 10（raw 13500 = 1350.0 V） |
| 理论路径 | mbpoll 写保持寄存器 → 控制线程(100ms) → 同址读回 |
| 预期 ΔT | ≤ **700 ms** |
| 脚本代跑 | `python3 scripts/perf/run_tc_r03.py` |

#### 操作步骤

1. 终端 B 启动 4.3 节 **yt4** 监控循环。
2. 运行脚本或 mbpoll 写寄存器（如 raw=13500），记录 **T0**。
3. 观察终端 B 首次读到相同 raw 值的 **T1**，计算 ΔT。
4. 更换写入值（13600、13700），重复 **3 次**。

#### 执行记录

> **代跑说明（2026-06-22）**：脚本写 rack 保持寄存器 addr 40004，T0 为写完成时刻；读回使用同址 `-t 4`。

| 次数 | 写入 raw | T0 | T1 | ΔT（ms） | 读回值 | 结果 |
|------|----------|-----|-----|----------|--------|------|
| 1 | 13500 | 17:19:03.401 | 17:19:03.428 | 27 | 13500 | ☑通过 ☐失败 |
| 2 | 13600 | 17:19:04.010 | 17:19:04.036 | 26 | 13600 | ☑通过 ☐失败 |
| 3 | 13700 | 17:19:04.635 | 17:19:04.662 | 27 | 13700 | ☑通过 ☐失败 |
| **统计** | — | — | — | avg=**26** max=**27** min=**26** | — | ☑通过 |

#### 现象描述

1. **触发与监控均为 rack yt4**，三轮 ΔT **26–27 ms**，**满足 ≤700 ms**。
2. bank 主站遥测（FC4 输入寄存器）外部不可写，故 BMS 模拟量选用 rack **FC6 遥调**点；与 TC-R01 bank 数字量互补，同属 BMS 域。
3. yt4 绑定簇模型门限，Modbus 写读延迟与线圈用例同量级（约 30 ms）。

---

### 5.4 TC-R04 — EMU yt2 功率因数模拟量变位响应

#### 用例定义

| 项 | 内容 |
|----|------|
| 目的 | 测量写入 **yt2** 后，**同一点位**在 Modbus 上反映的延迟 |
| 触发点 | simEmu1，slave **1**，yt2，地址 **40002**，端口 **1601** |
| 监控点 | **与触发点相同**（yt2 @1601/a1/40002，mbpoll `-t 4`） |
| 类型 | 模拟量 / 遥调（FC6 保持寄存器） |
| Scale | 1000（raw 950 = 0.950） |
| dpc 示例 | `dpc simEmu1.yt2 set 0.95` |
| 预期 ΔT | ≤ **700 ms** |
| 脚本代跑 | `python3 scripts/perf/run_tc_r04.py` |

#### 操作步骤

1. 终端 B 启动 4.4 节 **yt2** 监控循环。
2. 运行脚本或 `dpc simEmu1.yt2 set 0.9` / mbpoll 写 raw=900，记录 **T0**。
3. 记录 mbpoll 首次读到预期 raw 的 **T1**，计算 ΔT。
4. 换值（950、980）各测 **3 次**。

#### 执行记录

> **代跑说明（2026-06-22）**：脚本写 EMU 保持寄存器 addr 40002，监控同址读回。

| 次数 | 写入 raw | 物理量 | T0 | T1 | ΔT（ms） | 结果 |
|------|----------|--------|-----|-----|----------|------|
| 1 | 900 | 0.900 | 17:19:05.329 | 17:19:05.354 | 25 | ☑通过 ☐失败 |
| 2 | 950 | 0.950 | 17:19:05.950 | 17:19:05.978 | 28 | ☑通过 ☐失败 |
| 3 | 980 | 0.980 | 17:19:06.566 | 17:19:06.605 | 39 | ☑通过 ☐失败 |
| **统计** | — | — | — | — | avg=**30** max=**39** min=**25** | ☑通过 |

#### 现象描述

1. **触发与监控均为 yt2**，ΔT **25–39 ms**，**满足 ≤700 ms**。
2. yt2 **ModelSim=0**，写保持寄存器不经模型覆盖，适合 Modbus 层模拟量延迟基准测试。
3. 与 TC-R02 数字量（yx1 线圈）对比，模拟量寄存器写读延迟处于同一数量级。

---

### 5.5 TC-R05/R06 — PCS1 启停控制响应（yx3 → yc44）

#### 用例定义

| 项 | 内容 |
|----|------|
| 目的 | 测量 **mbpoll 写 PCS 启停** 后，**仿真器运行状态**在 Modbus 遥测上反映的延迟 |
| 控制点 | simEmu1，yx3，地址 **1003**，FC5 / `-t 0` |
| 反馈点 | simEmu1，yc44 单元1运行状态，地址 **10053**，FC4 / `-t 3` |
| 与 5.1～5.4 区别 | **非同点**：控制线圈 ≠ 状态寄存器，测的是闭环控制响应 |
| 启动成功判定 | 写 yx3=1 后 yc44 首次 ∈ **{1,3,4}** |
| 停机成功判定 | 写 yx3=0 后 yc44 首次 = **0** |
| 预期 ΔT_status | ≤ **700 ms** |
| 脚本代跑 | `python3 scripts/perf/run_tc_r05_pcs.py` |

#### 操作步骤

1. 确认 4.5 节前条件（yx0=1、无故障、可选写 yt0=500）。
2. 终端 B 启动 yx3 + yc44 双点监控循环。
3. **启动**：`mbpoll … 1003 … 1`，记录 **T0**；记录 yx3 读回 **T1_coil**、yc44 达标 **T1_status**。
4. **停机**：`mbpoll … 1003 … 0`，同样记录。
5. 启 / 停各 **10 次**。

#### 执行记录 — 启动（yx3=1 → yc44∈{1,3,4}）

> **代跑说明（2026-06-22 17:19，R01～R04 后复位 yx5/yx3，ControlEventDriven + 立即刷 OperationStatus，10 轮）**

| 次数 | T0 | ΔT_coil | T1_status | ΔT_status | 最终 yc44 | 结果 |
|------|-----|---------|-----------|-----------|-----------|------|
| 1 | 17:19:08.845 | 26 ms | 17:19:08.898 | **53** | 1（待机） | ☑通过 ☐失败 |
| 2 | 17:19:10.055 | 29 ms | 17:19:10.115 | **60** | 1（待机） | ☑通过 ☐失败 |
| 3 | 17:19:11.203 | 28 ms | 17:19:11.269 | **66** | 1（待机） | ☑通过 ☐失败 |
| 4 | 17:19:12.374 | 31 ms | 17:19:12.433 | **59** | 1（待机） | ☑通过 ☐失败 |
| 5 | 17:19:13.419 | 25 ms | 17:19:13.491 | **72** | 1（待机） | ☑通过 ☐失败 |
| 6 | 17:19:14.519 | 31 ms | 17:19:14.578 | **59** | 1（待机） | ☑通过 ☐失败 |
| 7 | 17:19:15.586 | 25 ms | 17:19:15.641 | **55** | 1（待机） | ☑通过 ☐失败 |
| 8 | 17:19:16.744 | 31 ms | 17:19:16.808 | **64** | 1（待机） | ☑通过 ☐失败 |
| 9 | 17:19:17.791 | 26 ms | 17:19:17.844 | **53** | 1（待机） | ☑通过 ☐失败 |
| 10 | 17:19:19.083 | 30 ms | 17:19:19.150 | **67** | 1（待机） | ☑通过 ☐失败 |
| **统计** | — | avg=**28** ms | — | avg=**60** max=**72** min=**53** | — | ☑通过 |

<details>
<summary>历史记录 — 16:03 代跑（立即刷遥测，轮询控制 100 ms，10/10）</summary>

启动 ΔT_status avg **281** ms（153–345 ms）；停机 avg **117** ms（86–188 ms）。

</details>

<details>
<summary>历史记录 — 16:02 首轮（未复位 yx5，启动 2/10）</summary>

未清除 bank yx5 过压告警时，多轮 yc44 停留在 **2（故障）** 或超时。

</details>

<details>
<summary>历史记录 — develop 分支 15:06 代跑（DataWorker 100 ms，10/10）</summary>

启动 ΔT_status avg **242** ms（161–344 ms）；停机 avg **170** ms（89–247 ms）。

</details>

#### 执行记录 — 停机（yx3=0 → yc44=0）

> **代跑说明（2026-06-22 17:19，同上 10 轮）**

| 次数 | T0 | ΔT_coil | T1_status | ΔT_status | 结果 |
|------|-----|---------|-----------|-----------|------|
| 1 | 17:19:21.257 | 67 ms | 17:19:21.359 | **102** | ☑通过 ☐失败 |
| 2 | 17:19:23.553 | 56 ms | 17:19:23.647 | **94** | ☑通过 ☐失败 |
| 3 | 17:19:25.777 | 53 ms | 17:19:25.858 | **81** | ☑通过 ☐失败 |
| 4 | 17:19:28.019 | 61 ms | 17:19:28.108 | **89** | ☑通过 ☐失败 |
| 5 | 17:19:30.253 | 70 ms | 17:19:30.352 | **99** | ☑通过 ☐失败 |
| 6 | 17:19:32.470 | 60 ms | 17:19:32.576 | **106** | ☑通过 ☐失败 |
| 7 | 17:19:34.692 | 54 ms | 17:19:34.772 | **80** | ☑通过 ☐失败 |
| 8 | 17:19:36.946 | 78 ms | 17:19:37.054 | **108** | ☑通过 ☐失败 |
| 9 | 17:19:39.226 | 56 ms | 17:19:39.324 | **98** | ☑通过 ☐失败 |
| 10 | 17:19:41.438 | 54 ms | 17:19:41.544 | **106** | ☑通过 ☐失败 |
| **统计** | — | avg=**61** ms | — | avg=**96** max=**108** min=**80** | ☑通过 |

<details>
<summary>历史记录 — 16:03 代跑（停机）</summary>

ΔT_status avg **117** ms（86–188 ms）。

</details>

<details>
<summary>历史记录 — develop 分支 15:06 代跑（停机）</summary>

ΔT_status avg **170** ms（89–247 ms）。

</details>

#### 现象描述

1. **启动 10/10 成功**：yc44 均进入 **{1,3,4}**（本轮多为 **1 待机**），无超时。
2. **ControlEventDriven + 立即刷 OperationStatus 后 ΔT_status ~60 ms**：启动 avg **60 ms**（53–72 ms）；停机 avg **96 ms**（80–108 ms）。较优化前（启动 avg **281 ms**）约 **快 4.7 倍**。
3. **ΔT_coil 约 25–78 ms**；ΔT_status 与 ΔT_coil 差值约 **30 ms**，主要为 mbpoll 50 ms 轮询 + 仿真刷遥测开销。
4. **TC-R05 前宜复位 yx5**：R01 触发过压告警后，基线 yc44 可能为 **2（故障）**，但不影响本轮启动判定（事件驱动后仍 10/10 成功）。

---

## 6. 测试结果汇总

| 用例 | 操作 | 监控点 | avg ΔT（ms） | max ΔT（ms） | 标准 | 结论 |
|------|------|--------|--------------|--------------|------|------|
| TC-R01 | `dpc simBms1.yx5 set 1` | yx5 @1501/a1/1005 | **31** | **35** | ≤700 | ☑通过 ☐失败 |
| TC-R02 | `dpc simEmu1.yx1 set 1` | yx1 @1601/a1/1001 | **26** | **27** | ≤700 | ☑通过 ☐失败 |
| TC-R02 | `dpc simEmu1.yx1 set 0` | yx1 @1601/a1/1001 | **29** | **31** | ≤700 | ☑通过 ☐失败 |
| TC-R03 | mbpoll 写 yt4 raw | yt4 @1501/a2/40004 | **26** | **27** | ≤700 | ☑通过 ☐失败 |
| TC-R04 | mbpoll 写 yt2 raw | yt2 @1601/a1/40002 | **30** | **39** | ≤700 | ☑通过 ☐失败 |
| TC-R05 | mbpoll 写 yx3=1 | yc44 @1601/a1/10053 | **60** | **72** | ≤700 | ☑通过 ☐失败 |
| TC-R06 | mbpoll 写 yx3=0 | yc44 @1601/a1/10053 | **96** | **108** | ≤700 | ☑通过 ☐失败 |

---

## 7. 测试结论

### 7.1 量化摘要

| 维度 | 最优 ΔT | 最差 ΔT | 是否达标 |
|------|---------|---------|----------|
| dpc → bank yx5（TC-R01） | **29** ms | **35** ms | ☑是 ☐否 |
| dpc → 低压断路器 yx1（TC-R02 合闸） | **25** ms | **27** ms | ☑是 ☐否 |
| dpc → 低压断路器 yx1（TC-R02 分闸） | **28** ms | **31** ms | ☑是 ☐否 |
| BMS rack yt4 模拟量（TC-R03） | **26** ms | **27** ms | ☑是 ☐否 |
| EMU yt2 模拟量（TC-R04） | **25** ms | **39** ms | ☑是 ☐否 |
| PCS1 停机 yc44（TC-R06） | **80** ms | **108** ms | ☑是 ☐否 |
| PCS1 启动 yc44（TC-R05） | **53** ms | **72** ms | ☑是 ☐否 |

### 7.2 总体结论

**TC-R01～TC-R06 全部通过。** 同点用例（R01～R04）ΔT 均在 **25–39 ms**。PCS 启停经 **ControlEventDriven + 立即刷 OperationStatus** 优化后，启动 ΔT_status avg **60 ms**、停机 avg **96 ms**（各 **10/10**），较优化前启动 **281 ms** 显著降低，均远小于 **700 ms** 标准。

---

## 8. 附录

### 8.1 备用 / 故障排查

**TC-R01：yx5 无变位或 ΔT 过大**

- 确认监控与触发均为 **slave 1 / addr 1005**，勿误读 rack 从站。
- dpc 路径理论上限约 **500 ms**（DataWorker）；mbpoll 写线圈路径通常 **<100 ms**。
- 若 dpc 无回显，检查 GUI 是否已进入「命令输入」（主界面按 `C` 或 `:`）。

**TC-R02：yx1 无变位或 dpc 无效**

- 确认监控与触发均为 **slave 1 / addr 1001 / 端口 1601**。
- yx1 **ModelSim=0**，优先用 **mbpoll 写线圈**或脚本 `run_tc_r02.py` 测 Modbus 层延迟。
- 若需测绑定模型的断路器点位，可另立用例（如 yx0 高压断路器），须遵守同点原则，不与 yx1 混测。

**TC-R03：yt4 无变位**

- 确认 **slave 2 / addr 40004 / 端口 1501**，mbpoll 读写的 `-t` 均为 **4**（FC6 保持寄存器）。
- rack 从站号 = 簇号 + 1（簇 0 → slave 2）。

**TC-R04：yt2 无变位**

- 确认 **slave 1 / addr 40002 / 端口 1601**，`-t 4` 读写。
- raw 值需按 Scale 1000 换算（0.950 → 950）。

**TC-R05/R06：启动无响应或 yc44=2**

- 确认 **yx0=1**（单元高压合闸）、并网点有网；必要时写 **yt0** 有功设定。
- 启动后若 **yx3 被清 0**，为联锁保护（无网/单元高压分/黑启动条件不满足），非 Modbus 延迟问题。
- **yc44=2** 表示故障态，需先排除告警再测启动。
- 主验收看 **yc44（OperationStatus）**，不是 yx3 线圈读回。

### 8.2 点位速查

| 用例 | 设备 | 点名 | FC | 地址 | 端口 | slave | mbpoll -t | 类型 |
|------|------|------|-----|------|------|-------|-----------|------|
| TC-R01 触发/监控 | simBms1 | yx5 (bank) | 5 | 1005 | 1501 | 1 | 0 | 数字量 |
| TC-R02 触发/监控 | simEmu1 | yx1 | 5 | 1001 | 1601 | 1 | 0 | 数字量 |
| TC-R03 触发/监控 | simBms1 | yt4 (rack) | 6 | 40004 | 1501 | 2 | 4 | 模拟量 |
| TC-R04 触发/监控 | simEmu1 | yt2 | 6 | 40002 | 1601 | 1 | 4 | 模拟量 |
| TC-R05/R06 控制 | simEmu1 | yx3 PCS1启停 | 5 | 1003 | 1601 | 1 | 0 写 | 控制 |
| TC-R05/R06 反馈 | simEmu1 | yc44 运行状态 | 4 | 10053 | 1601 | 1 | 3 读 | 反馈 |

### 8.3 原始日志

<details>
<summary>TC-R01 mbpoll 监控日志</summary>

```
（粘贴）
```

</details>

<details>
<summary>TC-R03 / TC-R04 mbpoll 监控日志</summary>

```
（粘贴）
```

</details>

<details>
<summary>GUI dpc 操作记录</summary>

```
（粘贴）
```

</details>

---

*报告版本：2026-06-22 | 用例：TC-R01~R06（同点变位 + PCS 控制响应）*
