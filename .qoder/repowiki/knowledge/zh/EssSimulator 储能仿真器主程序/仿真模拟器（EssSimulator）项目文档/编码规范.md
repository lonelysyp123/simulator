- 文档以 Markdown 表格形式列举配置项、API 路径或页面路由，便于快速查阅。
- 所有数值型配置（端口、点数、容量等）在文档中标注单位（kW/kvar/V/A/Hz/kVA），并以仓库 `appsettings.json` 为最终依据而非文档内示例数字。
- 文档之间通过相对路径互相链接，由 `docs/README.md` 作为统一入口索引。
- 对已废弃功能（如 TUI、`Runtime.NoGui`、旧名「充值版」）采用注释式说明，明确当前推荐用法与替代方案。