文档中约定的启动方式包括生产端 `./start.sh` / `start.bat`、开发端 `./scripts/dev-up.sh`（后端 `dotnet run --project EssSimulator.csproj` 配合前端 `cd Web && npm install && npm run dev`）、发布端 `./scripts/publish-windows.sh` / `publish-linux.sh`，以及通过环境变量 `Simulator__Web__HttpPort`、`Simulator__Web__ApiKey` 覆盖配置。