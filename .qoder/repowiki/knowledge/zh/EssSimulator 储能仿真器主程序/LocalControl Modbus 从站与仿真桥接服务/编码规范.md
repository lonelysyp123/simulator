- LC 侧寄存器到 EMU 侧参数名的映射通过 slot 奇偶（localPcs % 2 == 0 ? slot0 : slot1）选择不同字符串常量，并以固定偏移公式计算 LC 侧 paramN 地址。
- 控制下发前使用 TryReadChangedControl 配合 _controlShadow 字典做差量检测，首次读取用 double.NaN 标记未初始化，避免重复下发相同值。
- 对远端 ModbusSimServer 的读/写操作统一包裹 try/catch 并记录 Debug/Warn/Error 日志，异常时回退到默认值或恢复上一有效值（如 RevertInvalidHvBreakerCommand）。
- 全局控制（黑启动、高压断路器）采用 Prime*Shadow 预填充影子值 + 变更检测 + 广播到组内全部 emuIndex 的模式，成功才更新 _controlShadow。
- 数值转换集中通过 ModbusValueConverter.ToDouble 与 FormatControlValue 完成，禁止在各业务方法内自行解析原始对象。