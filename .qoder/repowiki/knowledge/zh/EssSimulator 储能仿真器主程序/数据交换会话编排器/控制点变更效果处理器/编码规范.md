- 每个效果类为 sealed 并实现 IControlEffect，通过返回枚举型 ControlEffectId 暴露自身标识。
- OnControlChanged 中优先通过静态 TryParseXxx(serverName, out) 方法校验并解析 serverName 前缀（simBms/simEmu），失败则直接 return 忽略。
- 通过 SimulatorHost.Instance.Get<T>(key) 获取 EnergyStorageSystem 或 EnergyManagementData 实例后再执行业务逻辑，空引用时静默返回。
- 对需要对外可见的状态变更在末尾调用 SnapshotService.RequestImmediatePush() 触发即时快照推送。