测试项目以 `EssSimulator.Tests.csproj`（net8.0、IsTestProject）为入口，通过 `<ProjectReference Include="..\EssSimulator.csproj" />` 引用被测主程序集，并依赖 xUnit、Microsoft.NET.Test.Sdk 与 coverlet.collector 进行断言与覆盖率收集。目录按被测子系统镜像划分命名空间：Battery、Bms、Devices、Propagation、Solver、Thermal、Topology、DataExchange、Protocol、Web、Licensing、Edition、Configuration、Model、Plant、LocalControl 等，每个子目录对应一个或多个被测类；跨子系统协作场景（如 ControlFeedbackPipeline）通过本地实现的 FakeSimulationAdapter / FakeModbusAdapter 等内嵌 fake 适配器隔离外部依赖。测试用例以 `[Fact]` 与 `[Theory] + [InlineData]` 为主，物理/数值收敛类测试采用固定迭代步数后断言区间（Assert.InRange），协议与中间件类测试则通过构造配置对象直接调用静态方法或构造函数进行白盒验证。