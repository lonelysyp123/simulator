Vue 3 `<script setup>` + Element Plus 表格/消息；Three.js (WebGL + CSS2DRenderer) 实现 3D 主接线，使用 ACES Filmic Tone Mapping 与 PCFSoftShadowMap；SVG 通过 `foreignObject` 嵌入 HTML 控件；实时数据通过 WebSocket 通道 `RealtimeChannels.MainLine` 推送。