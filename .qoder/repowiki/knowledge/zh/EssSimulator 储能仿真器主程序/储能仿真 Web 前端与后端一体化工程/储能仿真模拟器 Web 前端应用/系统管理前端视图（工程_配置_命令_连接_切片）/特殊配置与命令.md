`SystemConfigView` 在应用配置时会调用 `postSystemApply({ confirmRestart: true })` 并进入 `waitBackendReady` 轮询 `getHealth`，期间通过 `lockSystem/updateSystemProgress` 显示 18→96→100 的进度条；开发模式下需保证 `dev-up.sh` 已拉起后端以便重启后自动恢复。