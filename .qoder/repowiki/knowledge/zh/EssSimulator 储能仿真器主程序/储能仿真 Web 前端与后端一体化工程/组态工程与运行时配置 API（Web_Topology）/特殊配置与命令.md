文件持久化路径约定为 Web 内容根下的 `configs/topology/`（含 `project.json`、`projects/`、`library/`、`runtime-mode.json`、`generated/runtime-overlay.json`），启动时自动创建目录；应用工程模式后若 `ConfirmRestart=true`，会在异步延迟 400~500ms 后调用 `IHostApplicationLifetime.StopApplication()` 触发模拟器重启。