- 前端通过 services/api.js 集中发起 HTTP 请求，通过 services/useRealtime.js 订阅 SignalR 事件，视图层不直接耦合网络细节。
- 后端按功能域拆分为独立目录（Topology、DroopSlices、SnapshotReader*），每个域内以 Models/Endpoints/Store 分层组织代码。
- 前端路由与视图一一对应，views 下的 *.vue 文件通过 router.js 注册为页面入口。
- 前端使用 Vite alias '@' 指向 src/，组件与服务均以相对路径引用，避免深层相对导入。