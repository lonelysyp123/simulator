本地联调需执行 `./scripts/sync-pointmaps-to-root.sh [common|lc|battery]` 将选定版本的 CSV 同步到工作目录；发布时通过 `./scripts/publish-linux.sh <version>` 或 `./scripts/publish-windows.sh <version>`，也可通过环境变量 `POINTMAP_VERSION=lc` 指定版本；商业发布固定使用 `common` 版本且不可覆盖。