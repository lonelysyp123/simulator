# 储能仿真系统简要使用手册
## DPC指令（数据点变位）

### 1.1 概述
DPC（Data Point Change）指令用于读取和设置设备上的数据点值。通过此指令，工程人员可以直接修改仿真模型中的参数，用于测试系统反应。
- 不能控制FunctionCode为6的点位
- 不建议控制自带simModel的点位

### 1.2 基础指令

#### 单个数据点操作

**设置单个数据点值**
```
dpc <device>.<datapoint> set <value>
```

**示例**：
```
dpc simEmu1.yx0 set 1
dpc simBms1.yx10 set 100.5
```

**读取单个数据点值**
```
dpc <device>.<datapoint> get
```

**示例**：
```
dpc simEmu1.yx0 get
dpc simBms1.yx10 get
```

### 1.3 设备名称

| 设备代码 | 设备名称 | 说明 |
|---------|---------|------|
| `simEmu1` | 变压器协议模拟器 | PCS |
| `simBms1` | 电池系统协议模拟器 | BMS |

### 1.4 常见数据点示例

#### EMU设备
| 数据点 | 类型 | 范围 | 说明 |
|------|------|------|------|
| `param3` | 遥测 | int32 | 总有功功率 |
| `param4` | 遥测 | int32 | 总无功功率 |
| `param7` | 遥测 | int32 | 功率因数 |

#### BMS设备
| 数据点 | 类型 | 范围 | 说明 |
|------|------|------|------|
| `yc10` | 遥测 | int16 | 系统总电流 |
| `yc11` | 遥测 | u16 | 系统SOC |
| `yc12` | 遥测 | u16 | 系统SOH |

### 1.5 DPC操作模式说明

设置数据点时，只是向设备协议模拟器中写入，停留在协议层
- 当该点位没有绑定任何自动逻辑时（即simModel中没有符合规则的内容），设置的值将保持固定
- 当该点位绑定了相关逻辑时，系统会在下一个更新周期自动替换该点的值

---

## ESS命令（能量管理系统命令）

### 2.1 概述
ESS命令用于控制能量存储仿真系统的主要特性，包括pcs响应速度，上升斜率等。

### 2.2 PCS特性设置

#### 设置PCS1的特性

**语法**：
```
esscmd setPcs1 slope <value>      # 设置PCS功率调节速率
esscmd setPcs1 interval <value>   # 设置PCS功率调节间隔
esscmd setPcs1 delay <value>      # 设置PCS功率响应延时
```

**示例**：
```
esscmd setPcs1 slope 0.5          # 设置PCS1的功率调节速率为0.5（kw/ms）
esscmd setPcs1 interval 500       # 设置PCS1的每次功率变化间隔为500ms
esscmd setPcs1 delay 100          # 设置PCS1的初次响应延时为100ms
```

#### 设置负载特性

**语法**：
```
esscmd setLoad activePower <value>      # 设置负载的有功功率
esscmd setLoad reactivePower <value>    # 设置负载的无功功率
```

**示例**：
```
esscmd setLoad activePower 500          # 设置负载的有功功率为500kw
esscmd setLoad reactivePower 10         # 设置负载的无功功率为10kvar
```

### 3.4 操作流程示例

#### 场景1：调节PCS1的功率调节特性
```
1. (系统默认PCS功率调节特性曲线为 y=ax，默认的调节速率为0.5，间隔为500ms，延时为100ms)
2. (我想要加快PCS的调节，即增大速率，减少间隔，视情况而定延时)
3. esscmd setPcs1 slope 1
4. esscmd setPcs1 interval 400
5. (通过Modbus改变PCS有功功率设置值1000kw)
6. (pcs的功率变化为： .000ms 0kw -> .100ms 0kw -> .500ms 400kw -> .900ms 800kw -> 1.300ms 1000kw)
```

#### 场景2：特殊故障模拟
```
1. (系统正常运行中...)
2. dpc simEmu1.param100 get       # 前置条件：param100为故障字段，且没有simModel，当前状态为0
3. dpc simEmu1.param100 set 1
4. (观察系统响应...)
```

---

## 常见问题

### Q1: 设置的值在程序重启后会保留吗？

**A**: 否。设置的值只在内存中保存，程序重启后会重置。要长期保存配置，需要：
1. 修改配置文件

---

## 故障排除

### 故障1: 命令执行后显示"设备模型没有找到"

**原因**：
- 设备代码输入错误
- 设备未正确初始化
- 仿真服务器未完全启动

**解决方案**：
1. 检查设备名称拼写（simEmu/simBms）
2. 使用 `dpc/essscmd help` 命令查看可用设备
3. 查看日志信息确认设备初始化状态
4. 重启仿真服务器

### 故障2: 无法连接到Modbus服务器

**原因**：
- 端口被占用
- 防火墙阻止连接
- 服务器未正确启动

**解决方案**：
```bash
# 查看端口占用情况（Windows）
netstat -ano | findstr :1100

# 查看端口占用情况（Linux/macOS）
lsof -i :1100

# 关闭占用端口的进程后重新启动服务器
```

### 故障3: DPC数据点不存在

**原因**：
- 数据点名称拼写错误
- 数据点未在模型中定义
- 设备尚未加载配置文件

**解决方案**：
1. 检查配置文件中的数据点定义
2. 查看日志输出的详细错误信息
3. 确认配置文件路径正确

### 故障4: ESS命令执行后无反应

**原因**：
- 参数格式错误
- ESS系统未正确初始化

**解决方案**：
```
1. 检查参数格式：esscmd setPcs1 <数字> <数字>
2. 查看控制台输出信息
3. 重启ESS仿真服务器
```

---

## 技术参考

### 缩放处理

Modbus映射中，某些点位支持缩放（Scale）处理：
- CSV文件中的 `Scale` 列定义缩放倍数
- 例如 Scale=10 时，寄存器值 100 表示 10.0

---

## 更新历史

| 版本 | 日期 | 更改内容 |
|------|------|---------|
| 1.0 | 2026-01-22 | 初版发布 |

---

**文档维护**：如发现文档错误或有改进建议，请联系系统管理员。
